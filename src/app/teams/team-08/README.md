# Equipo 08

Trabajo de este grupo. El monolito ya da shell, login mock y `generic-ui`.

Reemplazá `team-08.page.ts` con las pantallas de su microservicio.
No toquen `src/app/layout`, `src/app/core` ni `src/app/pages` salvo acuerdo con quien mantiene el esqueleto.
